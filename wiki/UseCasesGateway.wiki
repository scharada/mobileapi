#summary Use Cases Gateway

= Use Cases Gateway =

View of the mobile application developer:

void send(channel, msg);
void receive(msg);



= Send =
void Logon(devID, token);

void Route(token, channel, msg);

void Logoff(devID, token);


= Receive =
void Receive(Msg msg);

 # Msg
 # ACK
 # NACK
 # UpdateToken
 # ChangeGateway