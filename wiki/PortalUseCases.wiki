== Portal Use Cases ==

 * email = CreateCompany?(name, email);
 * tmpPasswort, companyID = validateEmailLink(token, emailAdmin);
 * tmpPasswort = CreateSuperAdmin?(companyID, newAdminEmail, emailAdmin, secret);
 * AppId? = CreateApplication?(companyID, emailAdmin, secret);
 * tmpPasswort = CreateAppAdmin?(AppId, companyID, emailAdmin, secret);
 * pin = InviteUser?(companyID, AppId , emailAdmin, secret);
 * status = ChangPasswort?(emailAdmin, old, new);
 * blockUser(emailUser, AppId , emailAdmin, secret);
 * applications = Applictions(emailUser,  emailAdmin, secret);
 * deviceIDs = Devices(emailUser, appId,  emailAdmin, secret);
 * deviceStatus = Devicestatus(deviceID,  emailAdmin, secret)