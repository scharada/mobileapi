#summary Portal Use Cases

= Portal Use Cases =

  *  email = CreateCompany(name, email);

  * tmpPasswort, companyID = validateEmailLink(token, email);

  * tmpPasswort = CreateSuperAdmin(companyID, newAdminEmail, email, secret);

  * AppId = CreateApplication(companyID, email, secret);

  * tmpPasswort = CreateAdmin(AppId, companyID, email, secret);

  * status = ChangPasswort(email, old, new)