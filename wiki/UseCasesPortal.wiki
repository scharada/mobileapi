#summary Portal Use Cases

== Portal Use Cases ==

 * email = CreateCompany(name, email);
 * tmpPasswort, companyID = validateEmailLink(tok, email);
 * tmpPasswort = CreateSuperAdmin?(comID, newAdminEmail, email, pwd);
 * AppId? = CreateApp(comID, email, pwd);
 * tmpPasswort = CreateAppAdmin(AppID, comID, email, pwd);
 * pin = InviteUser(comID, AppId , email, pwd);
 * status = ChangePwd(email, pwd, newPwd);
 * blockUser(email, appID , email, pwd);
 * applications = Applictions(email,  email, pwd);
 * deviceIDs = Devices(email, appID,  email, pwd);
 * deviceStatus = Devicestatus(devID,  email, pwd)
 * users = GetUsers(comID, email, pwd)